# A Java Compiler

This is a project for the course "Compilers"

> [!IMPORTANT]
> You will need `make` to build the project.

## Build and test

`make build` - to build the project

`make test` - to run the tests

`make clean` - to clean the build

